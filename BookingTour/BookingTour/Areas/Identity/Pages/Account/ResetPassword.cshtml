@page
@model ResetPasswordModel
@{
    ViewData["Title"] = "Register";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="https://kit.fontawesome.com/64d58efce2.js" crossorigin="anonymous"></script>
    <link href="~/css/stylelogin.css" rel="stylesheet" />
    <title>Sign in & Sign up Form</title>
</head>
<body>
@*     @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger">
            @Model.ErrorMessage
        </div>
    }
    @{
        var alertMessage = TempData["AlertMessage"];
    }

    @if (alertMessage != null)
    {
        <div class="alert alert-warning">
            @alertMessage
        </div>
    } *@
    <div class="container">
        <div class="forms-container">
            <div class="signin-signup" >
                <form method="post" class="sign-in-form">
                    @* <div class="input-field">
                        <i class="fas fa-user" aria-hidden="true"></i>

                        <input type="password" class="form-control" id="pass" name="pass" required />
                    </div> *@
                    <div asp-validation-summary="ModelOnly" ></div>
               
                    <div class="input-field">
                        <i class="fas fa-user"></i>
                        <input asp-for="Input.Email" placeholder="Gmail đổi mật khẩu"  />
                        <span asp-validation-for="Input.Email"> </span>
                    </div>

                    <div class="input-field">
                        <i class="fas fa-user"></i>
                        <input asp-for="Input.Password" placeholder="Mật khẩu mới" />
                        <span asp-validation-for="Input.Password"> </span>
                    </div>

                    <div class="input-field">
                        <i class="fas fa-user"></i>
                        <input asp-for="Input.ConfirmPassword" placeholder="Mật khẩu xác minh" />
                        <span asp-validation-for="Input.ConfirmPassword"> </span>
                    </div>

                    <input type="hidden" asp-for="Input.Code" value="@Model.Input.Code" />

                    <button type="submit" class="btn btn-primary">Đặt lại mật khẩu</button>
                </form>
            @*     <form method="post" class="sign-in-form">
                    <div class="input-field">
                        <label for="Email">Email</label>
                        <input asp-for="Input.Email" class="form-control" />
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>

                    <div class="input-field">
                        <label for="Password">Mật khẩu mới</label>
                        <input asp-for="Input.Password" type="password" class="form-control" />
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>

                    <div class="input-field">
                        <label for="ConfirmPassword">Xác nhận mật khẩu mới</label>
                        <input asp-for="Input.ConfirmPassword" type="password" class="form-control" />
                        <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                    </div>

                    <button type="submit" class="btn btn-primary">Đặt lại mật khẩu</button>
                </form> *@
    </div>
</div>

        <div class="panels-container">
            <div class="panel left-panel">
                <div class="content">
                    <h3>Hello</h3>
                    <p>Please register your own account.</p>
                    <button class="btn transparent" id="sign-up-btn" onclick="window.location.href='/Identity/Account/Login'">Sign in</button>

                </div>
                <img src="~/img/log.svg" class="image" alt="" />
            </div>
            <div class="panel right-panel">
                <div class="content">
                    <h3>VIUVIU</h3>
                    <p>Please log in to your account!</p>
                    <button class="btn transparent" id="sign-in-btn" onclick="window.location.href='/Identity/Account/Login'">Sign in</button>
                </div>
                <img src="~/img/register.svg" class="image" alt="" />
            </div>
        </div>
    </div>
    <script src="~/js/app.js"></script>
    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
</body>
</html>
        